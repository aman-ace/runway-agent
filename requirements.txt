pandas>=2.0
numpy>=1.24
tabulate>=0.9
python-dotenv>=1.0

# Google's API (Gemini). This is the only cloud provider this build talks to.
google-genai>=0.3        # LLM_PROVIDER=gemini  (default)

# Optional local alternative, no key, no network.
# ollama>=0.3            # LLM_PROVIDER=ollama
